package oracle.jdbc.driver;

import org.junit.Test;

import static org.junit.Assert.*;

public class DynamicByteArrayTest {

    @Test
    public void createDynamicByteArray() {
        DynamicByteArray.createDynamicByteArray(null);
    }
}